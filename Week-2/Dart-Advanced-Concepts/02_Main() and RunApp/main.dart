//main() is the entry point of every Dart program.
//When you run a Dart program, execution starts from the main() function.

void main() {
  print("Hello");
}


/*runApp() is a Flutter function used to start the Flutter application's user interface
It takes a Widget as an argument and makes that widget the root of the widget tree.

Here:
runApp() → starts the Flutter UI.
MyApp → the root widget of the application.
const → creates the widget as a compile-time constant when possible.

main()
   ↓
runApp()
   ↓
Root Widget
   ↓
Widget Tree
   ↓
Flutter UI
*/


