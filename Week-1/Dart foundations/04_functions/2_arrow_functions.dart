//If a function contains only one expression
//An arrow function is a shorthand syntax for a function containing a single expression. 

void main(){
 int square(int number)=> number*number;
 print(square(5));
}